package cz.fel.cvut.sitforms.repository;

import cz.fel.cvut.sitforms.model.VariantAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VariantAnswerRepository extends JpaRepository<VariantAnswer, Long> {
}
